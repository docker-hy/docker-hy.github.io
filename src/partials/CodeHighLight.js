import React from "react";
import withSimpleErrorBoundary from "../util/withSimpleErrorBoundary";

const CodeHighLight = () => <div>Test</div>;

export default withSimpleErrorBoundary(CodeHighLight);
